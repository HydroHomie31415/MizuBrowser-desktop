diff --git a/browser/actors/AboutNewTabChild.sys.mjs b/browser/actors/AboutNewTabChild.sys.mjs
--- a/browser/actors/AboutNewTabChild.sys.mjs
+++ b/browser/actors/AboutNewTabChild.sys.mjs
@@ -50,6 +50,14 @@ export class AboutNewTabChild extends RemotePageChild {
         return; // about:newtab is a blank page
       }
 
+      // Mizu's small local watch list is ready before either Activity Stream
+      // renderer starts, so its React component never flashes in late.
+      let mizuWindow = Cu.waiveXrays(this.contentWindow);
+      mizuWindow.__MIZU_CONTINUE_WATCHING__ = Cu.cloneInto(
+        await this.sendQuery("Mizu:GetContinueWatching"),
+        this.contentWindow
+      );
+
       if (lazy.NEWTAB_REMOTE_RENDERER_ENABLED) {
         // We asynchronously request the parent to assign the right renderer
         // version to us, so that we can guarantee that the parent has locked
diff --git a/browser/actors/AboutNewTabParent.sys.mjs b/browser/actors/AboutNewTabParent.sys.mjs
--- a/browser/actors/AboutNewTabParent.sys.mjs
+++ b/browser/actors/AboutNewTabParent.sys.mjs
@@ -7,4 +7,5 @@ const lazy = {};
 ChromeUtils.defineESModuleGetters(lazy, {
   AboutNewTab: "resource:///modules/AboutNewTab.sys.mjs",
   ASRouter: "resource:///modules/asrouter/ASRouter.sys.mjs",
+  MizuContinueWatching: "resource:///modules/MizuContinueWatching.sys.mjs",
 });
@@ -108,8 +109,13 @@ export class AboutNewTabParent extends JSWindowActorParent {
         this.notifyActivityStreamChannel("onNewTabInit", message, tabDetails);
         break;
       }
 
+      case "Mizu:GetContinueWatching": {
+        let browser = this.browsingContext.top.embedderElement;
+        return lazy.MizuContinueWatching.entriesForBrowser(browser);
+      }
+
       case "Load": {
         this.notifyActivityStreamChannel("onNewTabLoad", message);
         this.makeTransientIfDisabledAndInitial();
         break;
diff --git a/browser/extensions/newtab/content-src/components/Base/Base.jsx b/browser/extensions/newtab/content-src/components/Base/Base.jsx
--- a/browser/extensions/newtab/content-src/components/Base/Base.jsx
+++ b/browser/extensions/newtab/content-src/components/Base/Base.jsx
@@ -16,6 +16,7 @@ import { Sections } from "content-src/components/Sections/Sections";
 import { Logo } from "content-src/components/Logo/Logo";
 import { Weather } from "content-src/components/Weather/Weather";
 import { WidgetsSidebar } from "content-src/components/Widgets/WidgetsSidebar";
+import { MizuContinueWatching } from "content-src/components/MizuContinueWatching/MizuContinueWatching";
 import { DownloadModalToggle } from "content-src/components/DownloadModalToggle/DownloadModalToggle";
 import { Notifications } from "content-src/components/Notifications/Notifications";
 import { TopicSelection } from "content-src/components/DiscoveryStreamComponents/TopicSelection/TopicSelection";
@@ -1132,6 +1133,10 @@ export class BaseContent extends React.PureComponent {
                   </>
                 )}
 
+                <ErrorBoundary>
+                  <MizuContinueWatching />
+                </ErrorBoundary>
+
                 {/* ASRouterNewTabMessage (ASROUTER_NEWTAB_MESSAGE_POSITIONS.ABOVE_TOPSITES) */}
                 {shouldShowASRouterNewTabMessage(
                   this.props.Messages,
@@ -1340,6 +1345,9 @@ export class BaseContent extends React.PureComponent {
                   </ErrorBoundary>
                 </div>
               )}
+              <ErrorBoundary>
+                <MizuContinueWatching />
+              </ErrorBoundary>
               {/* Bug 1914055: Show logo regardless if search is enabled */}
               {!prefs.showSearch && !noSectionsEnabled && <Logo />}
               <div className={`body-wrapper${initialized ? " on" : ""}`}>
diff --git a/browser/extensions/newtab/content-src/styles/activity-stream.scss b/browser/extensions/newtab/content-src/styles/activity-stream.scss
--- a/browser/extensions/newtab/content-src/styles/activity-stream.scss
+++ b/browser/extensions/newtab/content-src/styles/activity-stream.scss
@@ -160,6 +160,7 @@ panel-item::part(button) {
 @import '../components/Base/Grid';
 @import '../components/ErrorBoundary/ErrorBoundary';
 @import '../components/Logo/Logo';
+@import '../components/MizuContinueWatching/MizuContinueWatching';
 @import '../components/TopSites/TopSites';
 @import '../components/Sections/Sections';
 @import '../components/Search/Search';
diff --git a/browser/extensions/newtab/content-src/styles/nova/activity-stream.scss b/browser/extensions/newtab/content-src/styles/nova/activity-stream.scss
--- a/browser/extensions/newtab/content-src/styles/nova/activity-stream.scss
+++ b/browser/extensions/newtab/content-src/styles/nova/activity-stream.scss
@@ -155,6 +155,7 @@ panel-item::part(button) {
 @import '../../components/Base/Grid';
 @import '../../components/ErrorBoundary/ErrorBoundary';
 @import '../../components/Logo/Logo';
+@import '../../components/MizuContinueWatching/MizuContinueWatching';
 @import '../../components/TopSites/TopSites';
 @import '../../components/Sections/Sections';
 @import '../../components/Search/Search';
